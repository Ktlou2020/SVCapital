{
  "name": "svcapital-mobile",
  "version": "1.0.0",
  "description": "SV Capital Investor App — iOS & Android",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "sync": "npm run build && npx cap sync",
    "open:android": "npm run sync && npx cap open android",
    "open:ios": "npm run sync && npx cap open ios",
    "run:android": "npm run sync && npx cap run android",
    "run:ios": "npm run sync && npx cap run ios",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/haptics": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0",
    "@capacitor/network": "^7.0.0",
    "@capacitor/push-notifications": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0",
    "sharp": "^0.35.1"
  }
}
